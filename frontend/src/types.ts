export type DeckAssignment = { deck_id:string; deck_name:string; quantity:number }
export type Inventory = { id:string; card_name:string; set_code:string; set_name:string; collector_number:string; scryfall_id:string; quantity:number; foil:boolean; language:string; condition:string; purchase_price:number|null; market_price:number|null; previous_market_price:number|null; previous_price_recorded_at:string|null; storage_location:string; collection_name:string; image_url:string|null; notes:string|null; rarity:string|null; type_line:string|null; color_identity:string; status:string; date_added:string; deck_assignments:DeckAssignment[] }
export type Candidate = { scryfall_id:string; oracle_id?:string|null; name:string; set_code:string; set_name:string; collector_number:string; image_url:string|null; market_price:number|null; foil_market_price:number|null; finishes:string[]; language:string; confidence:number; color_identity?:string; rarity?:string|null; type_line?:string|null }
export type ScanResult = { disposition:'added'|'confirmation'|'suggestions'|'queued'|'empty'; confidence:number; inventory:Inventory|null; candidates:Candidate[]; review_id:string|null; message:string; processing_ms:number }
export type Defaults = { condition:string; foil:boolean; language:string; storage_location:string; collection_name:string; status:string; box_set_code:string|null; auto_add:boolean; deck_id:string|null }
export type DeckEntry = { id:string; quantity:number; inventory:Inventory }
export type Deck = { id:string; name:string; format:string|null; description:string|null; image_url:string|null; total_cards:number; unique_cards:number; total_value:number; created_at:string; updated_at:string; entries:DeckEntry[] }
export type AvailableCard = { inventory:Inventory; assigned_quantity:number; available_quantity:number }
export type AvailablePage = { items:AvailableCard[]; total:number; page:number; page_size:number }
export type DeckFormatSuggestion = { format:string; confidence:'high'|'possible'; reasons:string[] }
export type DeckFormatSuggestions = { complete_deck:boolean; card_count:number; suggestions:DeckFormatSuggestion[] }
export type AutoDeckCard = { inventory_id:string; scryfall_id:string; name:string; set_code:string; set_name:string; collector_number:string; image_url:string|null; quantity:number; role:string; mana_value:number; score:number; reasons:string[] }
export type AutoDeckProposal = { name:string; format:string; colors:string[]; strategy:string; theme:string; focus:string; focus_mode:'prefer'|'strict'; matched_sets:string[]; target_size:number; total_cards:number; complete:boolean; land_count:number; average_mana_value:number; synergy_score:number; quality_score:number; mana_sources:Record<string,number>; curve:Record<string,number>; role_counts:Record<string,number>; warnings:string[]; explanation:string[]; cards:AutoDeckCard[] }
export type GameCard = { instance_id:string; scryfall_id:string; name:string; rules_name?:string; image_url:string|null; type_line:string; oracle_text:string; mana_cost:string; mana_value:number; keywords:string[]; temporary_keywords?:string[]; power:string|null; toughness:string|null; effective_power?:number; effective_toughness?:number; loyalty?:string|null; tapped:boolean; damage:number; counters:Record<string,number>; regeneration_shields?:number; summoning_sick:boolean; commander?:boolean; token?:boolean; attached_to?:string }
export type GamePlayer = { id:string; name:string; is_bot:boolean; format:string; life:number; poison:number; firebending_mana?:number; hand:GameCard[]; hand_count?:number; library_count:number; battlefield:GameCard[]; graveyard:GameCard[]; exile:GameCard[]; command:GameCard[]; commander_casts:number; commander_damage:Record<string,number>; land_plays_remaining:number; kept_hand:boolean; mulligans:number; lost:boolean }
export type GameState = { version:number; status:'mulligan'|'active'|'complete'; winner_id:string|null; turn:number; phase:string; beginning_draw_pending?:boolean; first_turn_draw_skipped?:boolean; active_player_id:string; priority_player_id:string; players:GamePlayer[]; stack:{id:string;card:GameCard;controller_id:string;target_id:string|null;target_ids?:string[];mode_indices?:number[];mode_targets?:Array<string|null>;x_value?:number;kicked?:boolean;flashback?:boolean}[]; combat:{attackers:string[];blocks:Record<string,string>;attack_targets:Record<string,string>;block_orders:Record<string,string[]>;damage_pending:boolean}; consecutive_passes:number; pending_phase_advance:boolean; pending_discard:{player_id:string;amount:number}|null; pending_mulligan_bottom:string|null; pending_sacrifice:{player_id:string;amount:number;card_ids:string[]}|null; pending_legendary:{player_id:string;card_ids:string[]}|null; pending_library_search:{player_id:string;card_ids:string[];min_amount:number;max_amount:number;destination:string;tapped:boolean;label:string}|null; pending_scry:{player_id:string;amount:number;card_ids:string[]}|null; pending_damage_order:{player_id:string;groups:Record<string,string[]>}|null; pending_ward:{player_id:string;stack_id:string;cost_type:'mana'|'life'|'discard';mana_cost:string;amount:number;label:string;source_name:string}|null; pending_blight?:{player_id:string;amount:number;source_name:string}|null; pending_trigger_targets:{controller_id:string;source_name:string;card:GameCard}[]; log:{id:string;turn:number;message:string}[] }
export type GameTarget = { id:string; name:string; kind:'player'|'permanent'|'spell'|'card'; controller_id:string }
export type GameMode = { index:number;label:string;targets?:GameTarget[] }
export type LegalGameAction = { type:string; card_id?:string; card_name?:string; zone?:string; card_ids?:string[]; cards?:GameCard[]; groups?:{attacker:GameCard;blockers:GameCard[]}[]; amount?:number; min_amount?:number; max_amount?:number; destination?:string; tapped?:boolean; different_names?:boolean; shared_land_type?:boolean; reason?:string; targets?:GameTarget[]; target_steps?:{label:string;targets:GameTarget[];distinct?:boolean}[]; target_ids?:string[]; defenders?:GameTarget[]; ability_index?:number; label?:string; source?:string; source_name?:string; mana_cost?:string; cost_type?:'mana'|'life'|'discard'; cost_kind?:'discard'|'sacrifice'|'behold'|'crew'|'convoke'|'waterbend'|'blight'; cost_amount?:number; cost_min_amount?:number; cost_max_amount?:number; cost_required_power?:number; cost_options?:string[]; cost_combinations?:string[][]; cost_combinations_by_x?:Record<number,string[][]>; cost_card_ids?:string[]; self_sacrifice?:boolean; life_cost?:number; counter_cost?:{name:string;amount:number}|null; kicked?:boolean; kicker_cost?:string; convoke?:boolean; waterbend?:boolean; waterbend_amount?:number; blighted?:boolean; blight_amount?:number; mode_count?:number;mode_min?:number;mode_max?:number;mode_repeatable?:boolean;mode_distinct_targets?:boolean;modes?:GameMode[];chosen_modes?:number[];mode_targets?:Array<string|null>;x_min?:number;x_max?:number;x_value?:number; commander_tax?:number; legal_blocks?:Record<string,string[]> }
export type Game = { id:string; name:string; status:string; player_deck_id:string; opponent_deck_id:string; bot_difficulty:'beginner'|'standard'|'expert'; opponent_type:'bot'|'human'; invite_code:string|null; invite_token:string|null; host_token:string|null; invite_expires_at:string|null; state:GameState; legal_actions:LegalGameAction[]; created_at:string; updated_at:string }
export type ReferenceStatus = { state:'idle'|'running'|'complete'|'failed'; set_code:string|null; completed:number; total:number; catalog_total:number|null; indexed_cards:number; indexed_sets:number; fingerprinted_cards:number; variant_profiles:number; cached_images:number; coverage_percent:number|null; errors:number; error:string|null; updated_at:string|null; indexing_rate_per_second:number|null; estimated_seconds_remaining:number|null; estimated_completion_at:string|null }
