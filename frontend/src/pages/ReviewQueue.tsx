import { useEffect, useState } from 'react'; import { Button, Card, CardContent, Chip, Grid, Stack, Typography } from '@mui/material'; import { DeleteOutline, VisibilityOff } from '@mui/icons-material'; import { request } from '../api'; import type { Candidate } from '../types'
type Review={id:string;confidence:number;ocr_text:string|null;created_at:string;candidates:Candidate[]}
export default function ReviewQueue(){const [items,setItems]=useState<Review[]>([]);const load=()=>request<Review[]>('/reviews').then(setItems);useEffect(()=>{void load()},[]);const ignore=async(id:string)=>{await request(`/reviews/${id}/ignore`,{method:'POST'});load()};const remove=async(id:string)=>{await request(`/reviews/${id}`,{method:'DELETE'});load()};const resolve=async(item:Review,candidate:Candidate)=>{await request(`/reviews/${item.id}/resolve`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({candidate,defaults:{}})});load()};return <><Typography variant="h4">Review queue</Typography><Typography color="text.secondary" mb={3}>Hard scans wait here without interrupting a batch.</Typography><Grid container spacing={2}>{items.map(item=><Grid size={{xs:12,md:6}} key={item.id}><Card><CardContent><Stack direction="row" justifyContent="space-between"><Chip color={item.confidence>=70?'warning':'error'} label={`${item.confidence.toFixed(1)}% confidence`}/><Stack direction="row"><Button size="small" startIcon={<VisibilityOff/>} onClick={()=>ignore(item.id)}>Ignore</Button><Button size="small" color="error" startIcon={<DeleteOutline/>} onClick={()=>remove(item.id)}>Delete</Button></Stack></Stack><Typography variant="caption" color="text.secondary">OCR: {item.ocr_text||'No readable text'}</Typography>{item.candidates.map(c=><Stack key={c.scryfall_id} direction="row" spacing={2} alignItems="center" mt={2}><img src={c.image_url||''} width="55"/><div style={{flex:1}}><Typography fontWeight={700}>{c.name}</Typography><Typography variant="body2">{c.set_name} #{c.collector_number}</Typography></div><Button variant="contained" onClick={()=>resolve(item,c)}>Choose</Button></Stack>)}</CardContent></Card></Grid>)}</Grid>{!items.length&&<Typography color="text.secondary" textAlign="center" mt={8}>Nothing needs review.</Typography>}</>}
