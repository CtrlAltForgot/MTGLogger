services:
  db:
    volumes:
      - /mnt/user/appdata/mtglogger/postgres:/var/lib/postgresql/data
  api:
    volumes:
      - /mnt/user/appdata/mtglogger/data:/app/data
      - /mnt/user/appdata/mtglogger/paddlex:/root/.paddlex
